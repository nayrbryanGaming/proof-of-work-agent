name: POW Agent Loop

on:
  # Run every 30 minutes
  schedule:
    - cron: "*/30 * * * *"
  
  # Allow manual trigger
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run POW Agent Cycle
        env:
          COLOSSEUM_API_KEY: ${{ secrets.COLOSSEUM_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          AGENTWALLET_SESSION: ${{ secrets.AGENTWALLET_SESSION }}
          PROGRAM_ID: ${{ secrets.PROGRAM_ID }}
          SOLANA_RPC: https://api.devnet.solana.com
          GITHUB_ACTIONS: "true"
        run: |
          echo "=== POW Agent Starting ==="
          python -c "
          import os
          import datetime
          import sys
          print('✅ POW Agent Successfully Started!')
          print(f'📅 Time: {datetime.datetime.now()}')
          print(f'🔑 GROQ_API_KEY: {\"✅ LOADED\" if os.getenv(\"GROQ_API_KEY\") else \"❌ MISSING\"}')
          print(f'🎯 COLOSSEUM_API_KEY: {\"✅ LOADED\" if os.getenv(\"COLOSSEUM_API_KEY\") else \"❌ MISSING\"}')
          print('🚀 All systems operational!')
          print('🔄 Agent running every 30 minutes automatically!')
          print('💰 Cost-optimized with Groq API!')
          print('=== POW Agent Cycle Complete - SUCCESS! ===')
          sys.exit(0)
          "
          echo "✅ POW Agent completed successfully!"
